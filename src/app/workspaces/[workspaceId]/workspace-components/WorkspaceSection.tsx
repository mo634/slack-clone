import React from 'react'

const WorkspaceSection = ({ children }: { children: React.ReactNode }) => {
    return (
        <div>{
            children
        }</div>
    )
}

export default WorkspaceSection